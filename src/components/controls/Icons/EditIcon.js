import React, {PropTypes, Component} from 'react';
import cn from 'classnames';
import {Icon} from './Icon.js';

export const EditIcon = (props) => (
  <Icon fa="fa fa-pencil-square-o" {...props} />
);
